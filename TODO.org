#+TITLE: Obsidian Engine TODO

# TODO
List of everything flagged across the last commits:

* Cache System (all subsystems) [0/5]
- [ ] No cache invalidation — no mtime check, no version header, no magic version bump. A source file change silently loads stale data everywhere (.ogeom, .otex, .oibl, .omat, .orgb)
- [ ] Path sanitization collision — `./assets/foo/bar.gltf` and `./assets/foo_bar.gltf` produce the same cache key (affects geometry, texture, material caches)
- [ ] `get_texture_cache_path` uses a static buffer — calling it twice in the same scope (as done in `load_texture` for both the .dds and .otex paths) silently clobbers the first result
- [ ] `get_pipeline_cache_path`, `get_texture_cache_path` etc. should respect `XDG_CACHE_HOME` before falling back to `~/.cache`
- [ ] `mkdir` calls use `0777` — prefer `0755` to be explicit about intent (umask will handle it anyway but it's clearer)

* Geometry Cache (.ogeom) [0/3]
- [ ] `fread` return values in `create_mesh_from_primitive` cache hit path are unchecked — truncated file produces partial reads with no error
- [ ] `mesh.vertexCount` and `mesh.indexCount` are never set on the cache hit path due to brace restructuring — they stay 0 silently
- [ ] Indentation regression in the `fwrite` morph block in `gltf_loader.c`

* Mega Buffer [0/1]
- [ ] `megaMorphBufferAllocateFromFile` issues an immediate `beginSingleTimeCommands`/`endSingleTimeCommands` per mesh instead of using a `pendingMorphCopies` deferred batch list like the vertex and index variants do — serializes a GPU submit per morphed mesh on cache hit path

* Texture Cache (.otex / .dds) [0/3]
- [ ] JIT BC3 compressor only generates a single mip level — first-load DDS has `dwMipMapCount=1`, causing aliasing on surfaces in motion until cache is cleared
- [ ] DXGI format numbers are hardcoded as magic integers (`98`, `99`, `83`, etc.) — needs comments mapping them to `DXGI_FORMAT_*` names
- [ ] `fread` return value unchecked in the .otex cache hit path

* *Vulkan Pipeline Cache* [0/1]
- [ ] Pipeline cache header not validated before feeding to `vkCreatePipelineCache` — should check vendor ID, device ID, and UUID to handle GPU/driver switches safely rather than relying on driver robustness

* Rope / Text Editor [0/2]
- [ ] Fixed `tmp[4096]` / `tmp[16384]` buffers in `kill_line`, `kill_word`, `kill_region` — silently truncate on large regions
- [ ] Undo stack eviction uses `memmove` over the full 512-entry array — fine now but worth noting for future scaling

* Editor / Camera [0/3]
- [ ] `camera.active = false` set in both `camera_init` and `window.c` — one is redundant, should be removed
- [ ] Removal of `M-b/f/p/n` camera snap bindings should have a comment explaining they were ceded to the text editor keymap
- [ ] Magic constant `(16384 + 4096)` appears multiple times across files with no named constant — should be `#define MAX_INDIRECT_COMMANDS`

* IBL Cache (.oibl) [0/3]
- [ ] IBL dimensions (1024, 32, 128, 512) are hardcoded in three places — size calculations, region structs, and `create_ibl_image` calls. A named constant struct would make resolution changes safe
- [ ] Cache hit path allocates and holds a ~100MB `HOST_VISIBLE` staging buffer for the full duration of the upload — relevant if `loadIBL` is ever called at runtime for environment switching
- [ ] `fread` return value unchecked on the IBL cache hit path

* Material Cache (.omat) [0/1]
- [ ] If textures are added to a material directory after first scan, the OMAT cache hit returns stale paths forever

* General / Housekeeping [0/3]
- [ ] `static char cache_path[512]` declared mid-function in `load_gltf` after a `fclose` — mixed declarations and code, will warn or error depending on compiler flags. Should move to top of function or be extracted like `get_pipeline_cache_path`
- [ ] Indentation regression in `editor_update` — image viewer block got extra-indented
- [ ] `int w, h, ch;` at column 0 in `load_texture` — paste artifact, present since the .otex commit and still unresolved


Check quad utilization

#E07E3D - Folder
#372E2F - H1
missing

#70E03E - Folder
#313F37 - H1
missing

#3DBDDF - Folder
#2B3C4A - H1
#283542 - H2

#703DDF - Folder
#312D49 - H1
#2D2B42 - H2


* TODO Editor [2/5]
- [ ] Drag and drop for the FileSystem also show a green rounded quad when it's cached
- [ ] When moving position/rotation/scale from the inspector also show the guide lines
- [ ] When hovering the message don't make it disapper and lerp the border color to CT.border
- [X] Mesh picking and highlighting.
- [X] Inspector (runtime property editor) - Click mesh, edit PBR properties live with sliders. Ray vs AABB, writes directly into mapped SSBO memory, zero sync needed.

* TODO Bugs [1/3]
- [ ] The more time i'm alt tabbed from the engine the more time it will load once i tab back to it
- [ ] Panning up and down when loooking from upside down is wrong
- [X] Until i move the position of the camera, gltf loaded meshes will flicker

* TODO Correctness [1/2]
- [ ] The ease_table64 Bézier entries casting float function pointers to EaseFn64 is a known smell — worth a note.
- [X] The lines should not use PBR (Fixed via dedicated unlit SSBO slot and 0-instance indirect command).

* TODO Features [0/1]
- [ ] Parallax Occlusion Mapping - Togglable per material.
- [ ] GPU IK

* TODO Performance [2/7]
- [ ] Serialize IBLs, Materials, GLTFs and Pipelines on quit so it restarts istantly
- [ ] As I get closer to a texture the FPS drops drastically (Investigate mipmap generation, texture cache thrashing, or lack of anisotropic filtering limits).
- [ ] Hi-Z occlusion culling - Build a hierarchical depth buffer each frame, test mesh AABBs against it in the cull compute shader to reject occluded meshes before they hit the rasterizer.
- [ ] GPU Compute Animator - Move rigid node hierarchy and keyframe interpolation entirely to a Compute Shader to eliminate CPU bottlenecks.
- [ ] Split MeshGPUData SSBO - Separate static material data (textures, PBR factors) from dynamic transform data (`model`, `normalMatrix`) to save PCIe bandwidth during animations.
- [X] Hardware Skinning - Compute bone matrices and deform vertices in the vertex shader for skeletal meshes.
- [X] Unify immediate renderer into mega buffer - Dynamic geometry uses a resetting region; static meshes sit permanently behind it.

* TODO Gltf 2.0 [7/18]
- [X] Static meshes (Box, Corset, Avocado, Sponza).
- [X] PBR materials (albedo, normal, metallic/roughness, AO, emissive).
- [X] Morph targets (MorphStressTest).
- [X] Alpha mask / blend.
- [X] Unlit materials (UnlitTest).
- [X] Multiple texture coordinates (TextureCoordinateTest).
- [X] Embedded and external textures.
- [ ] Fix MetalRoughSpheres (Check metallic/roughness parsing or missing default factors).
- [ ] Fix AlphaBlendModeTest (Implement depth sorting for translucent meshes).
- [ ] Fix BoxAnimated / Fox (Animation channels: translation, rotation, scale interpolation).
- [ ] Fix AnimatedMorphSphere (Combine morph target weight animation with standard node transforms).
- [ ] Fix CesiumMan / RiggedFigure (Skeletal animation / joints / weights parsing).
- [ ] Fix MaterialsVariantsShoe (KHR_materials_variants support).
- [ ] Fix CarbonFibre / MosquitoInAmber (Advanced PBR extensions: Clearcoat, Transmission, Volume, IOR, Specular).
- [ ] KHR_texture_transform (UV scaling/offset).
- [ ] KHR_lights_punctual (Parse lights defined in the glTF file).
- [ ] Parse Cameras defined in the glTF file.
- [ ] Sparse accessors support.
# TODO Make sure those are right and add them all..




as you can see i heavily optimized everything What do you think are the next big wins in perfomance, i want to optimize EVERYTHING LIKE A MANIAC i really want to go in the rabbithole and squeeze every possible frame out of this engine it has to spend 0 time in the CPU it has to be AAA level it has to be super fast i want to focus on optimizations, NOTE that i also value ALOT refactors and removing lines count from a file what do you think we should do next ?


When you want me to modify code say EXACTLY find this in this file: and replace with EXACTLY this: with EXACT indentation and it has to be ONLY ASCII no invisible characters, you should also talk and say exactly what we are doing and why and what we should do next at the end, i want you to alaways behave like this when you want me to modify code, let's begin and let's do the RIGHT things that will make the architecture better and improve massivly performance let's begin!





Target 2: The Descriptor Set Massacre to fully eliminate UBOs and delete hundreds of lines of C code!

The Benefit for YOU:

    Simplified GPU Culling: In your cull.comp shader, you are currently writing out VkDrawIndexedIndirectCommand. With PVP, you can stop storing vertexOffset entirely. The shader just needs firstIndex and the base address of the vertex buffer (which you already pass as meshSSBOAddr via push constants!).

    Attribute Compression: Your Vertex struct (shown in fill3DVertexInput) uses 64 bytes per vertex (pos=12, color=16, normal=12, uv=8, tangent=16).
        With PVP, you can switch to f16vec2 for UVs and u8vec4 for Normals/Tangents.
        Result: You can likely fit a vertex in 32 bytes (down from 64).
        Impact: That megaVertexBuffer (e.g., 1GB) becomes 500MB. Memory bandwidth (often the main limiter in forward+ PBR) is cut in half.





;; TODO

- Meshlet culling in the cull shader
cull.comp operates per-mesh AABB. Splitting meshes into meshlets (64-triangle clusters) and culling per-meshlet gives you ~4-8x more culling granularity. Combined with VK_EXT_mesh_shader this is the path to truly AAA draw call budgets.

